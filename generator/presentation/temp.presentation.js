const <%= name %>Presentation = (props) => {
  //<div>{props.children}</div>;
  <div id="<%= name %>Presentation"><%= name %>Presentation</div>;
}
<%= name %>Presentation.propTypes = {
  //children: React.PropTypes.element.isRequired
};
<%= name %>Presentation.defaultProps = {
  //children: React.PropTypes.element.isRequired
};

export default <%= name %>Presentation;
